import { Header } from '../../components/Header';
import {
    Container,
} from './styles';

export const Dashboard = () => {

    return (
        <Container>
            <Header />
        </Container>
    );
}